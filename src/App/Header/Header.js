import React from "react";
import { Layout } from "antd";

const { Header } = Layout;

export default function HeaderComp() {
  return <Header style={{ padding: 0, backgroundColor: "#141414" }} />;
}
